version: "3.9"

services:
    b3-quant-analytics:
        build: .
        container_name: b3-quant-analytics
        env_file:
            - .env
        volumes:
            - ./:/app
        command: python src/main.py run
